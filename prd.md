# 📄 产品需求文档 (PRD)：AI 短剧剧本全自动生成流水线 (可视化编剧工作站)

## 1. 全景架构
本项目旨在为专业编剧打造一个无需配置环境、无需接触终端命令行的网页版剧本生产台。
* **后端核心：** 声明式 Agent 矩阵（爆款策划 Agent -> 分集架构师 Agent -> 酒馆对白驱动引擎）。
* **前端展示：** 基于 Python `Streamlit` 框架快速渲染的 Web 网页端。通过 `st.session_state` 保证编剧在网页上操作、修改大纲时，数据不丢失、不中断。

## 2. 核心编剧控场工作流 (Human-in-the-Loop)
为了让编剧以最低的工作量绝对控场，系统在网页端设置两个核心卡点：
1. **卡点一（百集主线审定）：** AI 凭空构思出大纲后，在网页端文本域（Text Area）呈现，编剧可直接修改，点击确认后解锁下一步。
2. **卡点二（分集挂钩点审定）：** AI 将故事切成 100 集的极简卡点清单（包含每集核心冲突与结尾悬念）。编剧在网页端审核该清单，微调确认后放行。
3. **全放行阶段：** 编剧点击最终生成，系统在后台多线程/异步并发调用 LLM 疯狂输出对白，前端网页以打字机流式（Streaming）动态刷新剧本。

## 3. 技术栈
* 前端 UI 与交互：`Streamlit` (全 Python 驱动的 Web 页面渲染)
* LLM 路由胶水：`LiteLLM` (支持流式传输 `stream=True`)
* 底层核心：大模型 Prompt 编排拓扑。

---

### 💬 接下来怎么对 CC（Claude Code）说？

请在终端直接对 CC 输入以下指令，它会直接帮你开始写网页和 Agent 逻辑：

“CC，我们最终确定了方案：**不让编剧用终端。我们要用 Streamlit 快速开发一个纯网页端的‘短剧 Agent 创作工作站’。后端采用 Agent 矩阵全自动构思故事，但要在网页上提供两个卡点供编剧修改和确认大纲，最终在网页上流式吐出剧本并支持一键下载。**

现在请帮我做以下两件事：
1. 理解我们最新的网页版 Agent 控场逻辑。
2. 在本地创建 `app.py`。利用 Streamlit 先把前端的框架搭建出来：包含题材输入框、大纲确认卡片（预留位置）、分集清单确认卡片（预留位置），以及右侧的剧本流式展示画布。
3. 在 `app.py` 中必须使用 `st.session_state` 来安全托管编剧输入的题材和大纲状态，防止编剧点击网页按钮时导致数据刷新丢失。

请先帮我写出这个前端 `app.py` 的初始漂亮结构！”